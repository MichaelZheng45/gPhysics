using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum rotationUpdate3D
{
    ROTATION_EULER_EXPLICIT,
    ROTATION_KINEMATIC
}

public enum positionUpdate3D
{
    POSITION_EULER_EXPLICIT,
    POSITION_KINEMATIC,
}

public class ParticleTresDimenciones : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
